const LikeGame = (userId,gameId) =>{
    // let url = get_game_records_url + "/${categoryKey}/${pageIndex}/${pageSize}";
    // let result = httphelper.httpget(url);
    return true;
}

module.exports = {
    LikeGame:LikeGame
}